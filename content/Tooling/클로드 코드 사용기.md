---
title: 클로드 설정해보기
date: 2025-08-04
draft: true
tags: 
banner: 
cssclasses: 
description: 
permalink: 
aliases: 
completed:
---
트렌드에 맞춰 에이전틱 코딩을 적용해보려한다.

기존까지 gemini, kiro, copilot 을 사용해봤고 현재 가장 핫하다는 claude code 를 사용해보려한다.

### 결제
앱스토어에서 pro 플랜 구매

### 설치
```bash
npm install -g @anthropic-ai/claude-code
```

### 사용량 모니터
클로드 토큰은 5시간 마다 갱신된다고 한다.
https://github.com/Maciek-roboblog/Claude-Code-Usage-Monitor
```bash
uv tool install claude-monitor
claude-monitor --plan pro
```
claude desktop 을 설치하지 않으면 `no claude data directory founded` 오류가 발생하는 듯 하다.
데이터가 부정확한듯하다. 사무실에서 쓰다가 집에와서 다른 피씨로 접근하면 수치가 다르게 뜬다

![[스크린샷 2025-08-04 오후 7.01.04.png]]

### 서브 에이전트 설정
클로드를 선택해본 가장 큰 이유중 하나다


### MCP 설정
```bash
# serena
claude mcp add serena -- uvx --from git+https://github.com/oraios/serena serena start-mcp-server --context ide-assistant --project $(pwd)

claude mcp add --transport http context7 https://mcp.context7.com/mcp

claude mcp add playwright npx @playwright/mcp@latest
```
명령어를 실행하고 claude 를 다시 실행시켜서 `/mcp` 를 프롬프트에 입력해보면 현재 연결중이라는 메세지를 확인할 수 있다.

onboarding 을 입력해주면 `.serena` 폴더가 생성됨

### 테스트
일단은 무지성으로 빈 프로젝트를 만들고 "해줘!" 를 시전했다.
cpu 리소스를 사용한 rag 백엔드를 만들어 달라고 요구사항을 md 로 정의해서 전달하고
/init 명령어로 프로젝트를 초기화했다.
문서가 한글로 되어있었지만. CLAUDE.MD 는 영어로 작성해줬다.
일단 서브에이전트는 동작하지 않았고
serena 도 프로젝트 구조가 없다는 이유로 init 되지 않는 것 같다
```bash
⏺ 먼저 현재 프로젝트 상태를 파악해보겠습니다.

⏺ serena - list_dir (MCP)(relative_path: ".", recursive: false)
  ⎿  Error: No active project. Ask to user to select a project from this list: []

⏺ 프로젝트가 활성화되지 않았습니다. 현재 디렉토리를 활성화하겠습니다.

⏺ serena - activate_project (MCP)(project: "/Users/taesoo/projects/rag4me-cla")
  ⎿  Error executing tool: No source files found in /Users/taesoo/projects/rag4me-cla

     To use Serena with this project, you need to either:
     … +8 lines (ctrl+r to expand)

⏺ 현재 프로젝트에 소스 파일이 없는 상태입니다. 일반 도구들을 사용해서 프로젝트 상태를
  확인해보겠습니다.
```
도큐먼트도 만들어주고 메소드에 한글 주석도 달아달라 해야겠다

토큰이 터진다는데 아직 답변이 멈추진 않고있다.
```
📊 Token Usage:          🔴 [██████████████████████████████████████████████████] 249.8%    47,469 / 19,000

📨 Messages Usage:       🟢 [████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 24.4%    61 / 250
────────────────────────────────────────────────────────────
⏱️  Time to Reset:       🟢 [███████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 3h 29m

🤖 Model Distribution:   🤖 [██████████████████████████████████████████████████] Sonnet 100.0%
────────────────────────────────────────────────────────────
🔥 Burn Rate:              527.4 tokens/min ⚡
💲 Cost Rate:              $0.0224 $/min

```


이쯤에서 터졌다
```bash
💰 Cost Usage:           🟢 [█████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 42.4%    $7.63 / $18.00

📊 Token Usage:          🔴 [██████████████████████████████████████████████████] 466.0%    88,531 / 19,000

📨 Messages Usage:       🟡 [████████████████████████████████████░░░░░░░░░░░░░░] 73.6%    184 / 250
────────────────────────────────────────────────────────────
⏱️  Time to Reset:       🟢 [████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 2h 58m

🤖 Model Distribution:   🤖 [█████████████
```

`Claude usage limit reached. Your limit will reset at 10pm (Asia/Seoul).`

엄청 야박하지도 엄청 넉넉하지도 않은 그런 사이즈 같은데 이건 조금 더 써봐야 할 듯



---

2회차
이제는 프로젝트 구조도 생겼겠다 serena onboarding 을 진행하고 시작한다.
생각보다 온보딩 시간이 걸리는구나

`/compact` 를 이용해서 컨텍스트를 압축시켜버리기

외부 웹 검색도 해달라고 했는데 욘석이 내부 디비만 검색하게 해뒀네
다시 요청 고고

아찔하네 진짜 1시간만에 토큰 빠이빠이~ 네시간 손가락빨기 ㅋㅋㅋ

세션 나갔다가 이전 컨텍스트 가져올때는 `/resume`

### gemini 와 협업시키기

```markdown
# Using Gemini CLI for Large Codebase Analysis

When analyzing large codebases or multiple files that might exceed context limits, use the Gemini CLI with its massive
context window. Use `gemini -p` to leverage Google Gemini's large context capacity.

## File and Directory Inclusion Syntax

Use the `@` syntax to include files and directories in your Gemini prompts. The paths should be relative to WHERE you run the
  gemini command:

### Examples:

**Single file analysis:**
gemini -p "@src/main.py Explain this file's purpose and structure"

Multiple files:
gemini -p "@package.json @src/index.js Analyze the dependencies used in the code"

Entire directory:
gemini -p "@src/ Summarize the architecture of this codebase"

Multiple directories:
gemini -p "@src/ @tests/ Analyze test coverage for the source code"

Current directory and subdirectories:
gemini -p "@./ Give me an overview of this entire project"

# Or use --all_files flag:
gemini --all_files -p "Analyze the project structure and dependencies"

Implementation Verification Examples

Check if a feature is implemented:
gemini -p "@src/ @lib/ Has dark mode been implemented in this codebase? Show me the relevant files and functions"

Verify authentication implementation:
gemini -p "@src/ @middleware/ Is JWT authentication implemented? List all auth-related endpoints and middleware"

Check for specific patterns:
gemini -p "@src/ Are there any React hooks that handle WebSocket connections? List them with file paths"

Verify error handling:
gemini -p "@src/ @api/ Is proper error handling implemented for all API endpoints? Show examples of try-catch blocks"

Check for rate limiting:
gemini -p "@backend/ @middleware/ Is rate limiting implemented for the API? Show the implementation details"

Verify caching strategy:
gemini -p "@src/ @lib/ @services/ Is Redis caching implemented? List all cache-related functions and their usage"

Check for specific security measures:
gemini -p "@src/ @api/ Are SQL injection protections implemented? Show how user inputs are sanitized"

Verify test coverage for features:
gemini -p "@src/payment/ @tests/ Is the payment processing module fully tested? List all test cases"

When to Use Gemini CLI

Use gemini -p when:
- Analyzing entire codebases or large directories
- Comparing multiple large files
- Need to understand project-wide patterns or architecture
- Current context window is insufficient for the task
- Working with files totaling more than 100KB
- Verifying if specific features, patterns, or security measures are implemented
- Checking for the presence of certain coding patterns across the entire codebase

Important Notes

- Paths in @ syntax are relative to your current working directory when invoking gemini
- The CLI will include file contents directly in the context
- No need for --yolo flag for read-only analysis
- Gemini's context window can handle entire codebases that would overflow Claude's context
- When checking implementations, be specific about what you're looking for to get accurate results
```

https://www.reddit.com/r/ChatGPTCoding/comments/1lm3fxq/gemini_cli_is_awesome_but_only_when_you_make/

### 플랜 모드???
이거 해봐야한다

```
Generated with [Claude Code](https://claude.ai/code)
Co-Authored-By: Claude <noreply@anthropic.com>
```

## 클로드가 커밋에 지이름을 박네?! (사실 맞긴해)
하지만 없애는 방법도 찾아봐야겠지

https://docs.anthropic.com/ko/docs/claude-code/settings

글로벌 설정 `~/.claude/settings.json` 에 다음 옵션을 추가한다.
```json
"includeCoAuthoredBy": false
```

설정해주고도 여전히 'Co-Authored-By' 를 입력하려길래 설정을 확인해달라니 그제서야 지운다 (원래 그런가)

## 클로드가 어느순간 어마어마하게 토큰을 먹더라?
계속하기 신공하면 10분도 되지않아서 토큰초과 ㅋㅋ

단순 버그인줄 알았는데 프로젝트 구조가 커져서 그런가
프롬프트 쓰는 방식을 개선해봐야 할 것 같음

## 클로드 코드 설치 오류
갑자기 종료하고 재시작하려니 안되더라
```bash
node:internal/modules/cjs/loader:1410 
   const err = new Error(message); 
               ^ 
               
Error: Cannot find module './yoga.wasm' 
Require stack: 
- /opt/homebrew/lib/node_modules/@anthropic-ai/claude-code/cli.js
  at Module._resolveFilename (node:internal/modules/cjs/loader:1410:15) 
  at require.resolve (node:internal/modules/helpers:163:19) 
  at file:///opt/homebrew/lib/node_modules/@anthropic-ai/claude-code/cli.js:856:22853 
  at ModuleJob.run (node:internal/modules/esm/module_job:371:25) 
  at async onImport.tracePromise.__proto__ (node:internal/modules/esm/loader:669:26) 
  at async asyncRunEntryPointWithESMLoader (node:internal/modules/run_main:101:5) { 
  code: 'MODULE_NOT_FOUND', 
  requireStack: [ 
      '/opt/homebrew/lib/node_modules/@anthropic-ai/claude-code/cli.js' 
	] 
}
```

원인은 모르겠고 재설치해주니 된다.
```
# 전역 설치를 완전히 삭제 (실패해도 무시)
npm uninstall -g @anthropic-ai/claude-code --force 

# 폴더 수동 삭제 
rm -rf /opt/homebrew/lib/node_modules/@anthropic-ai/claude-code

# 클로드 코드 재설치
npm install -g @anthropic-ai/claude-code
```

[https://github.com/anthropics/claude-code/issues/1103](https://github.com/anthropics/claude-code/issues/1103)

## 