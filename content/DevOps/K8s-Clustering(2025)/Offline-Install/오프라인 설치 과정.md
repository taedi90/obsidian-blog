---
title: 오프라인 설치 과정
date: 2025-07-21
draft: true
tags: 
banner: 
cssclasses: 
description: 
permalink: 
aliases: 
completed: 
type:
  - note
---
![](https://i.imgur.com/oDdTQl4.png)

```mermaid
---
config:
  theme: redux
---
flowchart TB
    subgraph on["Online"]
        direction LR
            n1["인벤토리 설정"]
            n2["파일 다운로드"]
            n3["패키징"]
    end
    subgraph off["Offline"]
        direction LR
        subgraph agent["Agent Node"]
            direction TB
                a1["도커 설치"]
                a2["nginx"]
                a3["docker registry"]
                a4["ansible"]
        end
        subgraph cluster["Cluster Node"]
            direction TB
                c1["오프라인 repo 설정"]
                c2["리눅스 패키지 설치"]
                c3["컨테이너 이미지 로드"]
                c4["kubernetes 클러스터링"]
        end
    end
        on --> agent --> cluster
        n1 --> n2 --> n3
        a1 --> a2
        a1 --> a3
        a1 --> a4
        c1 --> c2 --> c4
        c3 --> c4

```
## 온라인 환경
### 사전
- ansible inventory 구성
- plugin component config 구성

### 패키징
- 패키지 준비 (linux package, container image, kubernetes binary)
- 패킹 (packages, script, plugin component helm charts)

## 오프라인 환경
### 에이전트 노드 셋업
- containerd 설치
- nginx 컨테이너 실행 (linux package, container image, kubernetes binary)
- docker registry 실행 (kubespray image)
	- 사전에 노드에 이미지 load 상태면 레지스트리 준비안해도 되는지 확인 필요
- ansible 컨테이너
	- sleep 99d
	- dind 필요여부 확인

### 클러스터링
- 오프라인 repo 설정
- 리눅스 패키지 설치
- 컨테이너 이미지 로드
- kubespray 클러스터링
- (taint 제거)
- 플러그인 컴포넌트 설치
